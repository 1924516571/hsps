<template>
    <slider :class="wrapClass" v-bind="$attrs" v-on="$listeners"></slider>
</template>

<script lang="ts">
    import { Component, Vue } from 'vue-property-decorator';
    import { Slider } from '../../element';
    import { ELEMENT_CLASS_PREFIX } from '../constants';

    @Component({
        components: {
            Slider
        }
    })
    export default class SucSlider extends Vue {
        wrapClass: string = `${ELEMENT_CLASS_PREFIX}slider`;
    }
</script>

