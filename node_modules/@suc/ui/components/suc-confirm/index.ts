import { Modal, ModalConfig } from 'view-design';

let SucConfirm = (<any>Modal).confirm;
export {
    SucConfirm
};
export default SucConfirm;
declare module 'vue/types/vue' {
    interface Vue {
        /**
         * 全局提示
         */
        $SucConfirm: (config?: ModalConfig | string) => void;
    }
}
