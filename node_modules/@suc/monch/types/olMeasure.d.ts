import Vue from 'vue';

export declare class OlMeasure extends Vue {
    /**
     * 测距 or 测面
     * @default LineString
     */
    type?: 'LineString'|'Polygon';
    /**
     * 交互再次触发的信号
     * @default LineString
     */
    triggerNew?: string|number;
    /**
     * 图层层级
     * @default 0
     */
    zIndex?: number;

    /**
     * 测量结束时触发
     */
    $emit(eventName: 'drawend'):this;
}
