import Vue from 'vue'
import {Style} from './style';

export declare class OlRadar extends Vue {
    /**
     * 中心点坐标
     */
    center: number[];
    /**
     * 半径尺寸 单位米
     * @default 500
     */
    radius?: number;
    /**
     * 圆圈样式
     * @default {
                        stroke: {
                            width: 1,
                            color: '#12b5d0'
                        }
                    }
     */
    circleStyle?: Style;
    /**
     * 扇形颜色
     * @default #12b5d0
     */
    sectorColor?: string;
    /**
     * 动画速度
     * @default normal
     */
    speed?: 'slow'|'normal';
    /**
     * 图层层级
     * @default 0
     */
    zIndex?: number;
    /**
     * 输入输出数据采用的坐标系
     * @default EPSG:4326
     */
    projection?: string;
}
