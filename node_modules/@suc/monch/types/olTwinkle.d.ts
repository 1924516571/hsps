import { Style } from './style';
import {VectorLayer} from "./vectorLayer";

export declare class OlTwinkle extends VectorLayer {
    /**
     * 中心点坐标
     */
    coords: number[];
    /**
     * 点样式
     * @default {
                        image: {
                            circle: {
                                radius: 14,
                                fill: {
                                    color: [255, 0, 0, 1]
                                }
                            }
                        }
                    }
     */
    circleStyle?: Style;
    /**
     * 一个动画周期
     * @default 1000
     */
    duration?: number;
    /**
     * 半径尺寸
     * @default 10
     */
    radius?: number;
    /**
     * 图层层级
     * @default 0
     */
    zIndex?: number;
    /**
     * 输入输出数据采用的坐标系
     * @default EPSG:4326
     */
    projection?: string;
}
