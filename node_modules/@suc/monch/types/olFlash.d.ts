import Vue from 'vue'
import { Style } from './style';
import {VectorLayer} from "./vectorLayer";

export interface CenterInterface {
    radius: number;
    color: number[]|string
}

export declare class OlFlash extends VectorLayer {
    /**
     * 中心点坐标
     */
    coords: number[];
    /**
     * 点样式
     * @default {
                        image: {
                            circle: {
                                radius: 0,
                                fill: {
                                    color: [255, 0, 0]
                                }
                            }
                        }
                    }
     */
    circleStyle?: Style;
    /**
     * 一圈的动画周期
     * @default 3000
     */
    duration?: number;
    /**
     * 圈数
     * @default 3
     */
    loops?: number;
    /**
     * 半径变化范围
     * @default [0, 30]
     */
    radiusRange?: [number, number];
    /**
     * 中心是否有固定半径的圆圈
     */
    hasCenter?: CenterInterface;
    /**
     * 图层层级
     * @default 0
     */
    zIndex?: number;
    /**
     * 输入输出数据采用的坐标系
     * @default EPSG:4326
     */
    projection?: string;
}
