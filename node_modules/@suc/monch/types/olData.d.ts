import type Map from 'ol/Map';

interface OlData {
    setMap(olMap: Map, id: string): void;

    getMap(id: string): Promise<Map>;

    resetMap(id: string): void;
}

declare const olData: OlData;
export default olData;
