import Vue from "vue";
import {Extent} from "ol/extent";
import {OrderFunction} from "ol/render";
import {StyleLike} from "./style";

export declare class VectorLayer extends Vue{
    className?: string;
    opacity?: number;
    visible?: boolean;
    extent?: Extent;
    zIndex?: number;
    minResolution?: number;
    maxResolution?: number;
    minZoom?: number;
    maxZoom?: number;
    renderOrder?: OrderFunction;
    renderBuffer?: number;
    declutter?: boolean;
    updateWhileAnimating?: boolean;
    updateWhileInteracting?: boolean;
    /**
     * 图层是否在初始化时自适应屏幕
     * @default false
     */
    isFit?: boolean;
    fitPadding: [number, number, number, number];  // 仅在 isFit 为 true 时起作用
    /**
     * 图层id 可通过 layer.get('id') 获取
     */
    layerId?: string;
    /**
     * 图层样式
     */
    layerStyle?: StyleLike;
    /**
     * 图层渲染方式 'image' 性能更好，但是地图缩放动画时图标和文字也会缩放
     * @default vector
     */
    renderMode?: 'vector' | 'image';
}
