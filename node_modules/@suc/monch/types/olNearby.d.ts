import {Style} from './style';
import {VectorLayer} from "./vectorLayer";

export declare class OlNearby extends VectorLayer {
    /**
     * 中心点坐标
     * @default [120, 30]
     */
    center?: number[];
    /**
     * 半径尺寸 单位米  双向绑定
     * @default 0
     */
    radius?: number;
    /**
     * Circle 样式
     * @default {
                        fill: {
                            color: [110, 162, 228, 0.3]
                        },
                        stroke: {
                            width: 1,
                            color: [110, 162, 228]
                        }
                    }
     */
    circleStyle?: Style;
    /**
     * 图层层级
     * @default 0
     */
    zIndex?: number;
    /**
     * 输入输出数据采用的坐标系
     * @default EPSG:4326
     */
    projection?: string;

    /**
     * 半径更新时触发
     */
    $emit(eventName: 'update:radius', radius: number):this;
}
